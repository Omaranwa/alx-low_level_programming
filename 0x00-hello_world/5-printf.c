#include <stdio.h>

/**
 * main - Entry point for The Program
 * another method to print strings (aka: printf)
 * Return: Always 0 (Success)
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
