#include <stdio.h>

/**
* main - main block of code
* using for - if digit is less than 10
* print it - add 1 to it - loop
* end win digit is bigeer than 10
* Return: Always 0 (Success)
*/
int main(void)
{
	int digit;

	for (digit = 0; digit < 10; digit++)
	printf("%i", digit);
	putchar('\n');

	return (0);
}
