#include <stdio.h>
#include <stdlib.h>
/**
 * main - main block
 * a program that prints all possible
 */
int main(void)
{
	int comb;
	int desk = 0;

	while (desk < 10)
	{
		comb = 0;
		while (comb < 10)
		{
			if (desk != comp && desk < comb)
			{
				putchar('0' + desk);
				putchar('0' + comb);

				if (comb + desk != 17)
				{
					putchar(',');
					putchar(' ');
				}
			}

			c++;
		}
		d++;
	}
	putchar('\n');
	return (0);
}
